{
  "name": "@jiaul.islam/common.ticketing.dev",
  "version": "1.0.12",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "./build/**/*"
  ],
  "scripts": {
    "clean": "rm -rf ./build && rm -rf ./dist",
    "build": "npm run clean && tsc",
    "pub": "npm run build && npm version patch && npm publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Common utilities and types for the ticketing application",
  "dependencies": {
    "@confluentinc/kafka-javascript": "^1.4.0",
    "@types/cookie-session": "^2.0.49",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "cookie-session": "^2.1.1",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "http-status-codes": "^2.3.0",
    "jsonwebtoken": "^9.0.2",
    "typescript": "^5.9.2"
  }
}
